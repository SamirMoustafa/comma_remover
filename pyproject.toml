[project]
name = "comma-remover"
version = "0.1.0"
description = "Remove trailing commas from Python files"
requires-python = ">=3.8"

[project.scripts]
remove-commas = "comma_remover:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"